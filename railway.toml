[build]
builder = "RAILPACK"
buildCommand = "./gradlew clean bootJar"

[deploy]
startCommand = "java -jar build/libs/Flowterserver-0.0.1-SNAPSHOT.jar"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10